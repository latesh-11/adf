properties([pipelineTriggers([upstream(
    threshold: hudson.model.Result.SUCCESS,
    upstreamProjects: "test_A/mian")])])

pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo "Current branch: ${env.BRANCH_NAME}"
                echo "Build Cause: ${env.BUILD_CAUSE}"
                echo 'Hello World'
            }
        }
    }
}
